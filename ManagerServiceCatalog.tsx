import { TabPlaceholder } from "./TabPlaceholder";

// Placeholder — real content goes here. Service Catalog holds the rate-card /
// pricing data the Manager uses to build quotes. Opens in a Modal from the
// Manager account menu. Replace the body when you build it.
export function ManagerServiceCatalog() {
  return <TabPlaceholder title="Service Catalog" />;
}
