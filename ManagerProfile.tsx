import { TabPlaceholder } from "./TabPlaceholder";

// Placeholder — real content goes here. Creator Profile holds the business
// identity (name, address, bank, tax) that feeds invoices and PDFs. Opens in
// a Modal from the Manager account menu. Replace the body when you build it.
export function ManagerProfile() {
  return <TabPlaceholder title="Creator Profile" />;
}
