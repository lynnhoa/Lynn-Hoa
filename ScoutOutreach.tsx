import { TabPlaceholder } from "./TabPlaceholder";

// Placeholder — real content goes here. Replace the body of this component
// when you build the Outreach view; the frame (logo, tabs, account menu) is
// handled by RoleLayout and never needs to be touched here.
export function ScoutOutreach() {
  return <TabPlaceholder title="Outreach" />;
}
